#pragma once

#include "Agents.h"
/*
class FSM;

class State
{
public:

	virtual void Update(Agents agent, FSM sm);
	virtual void Init(Agents agent);
	virtual void Exit(Agents agent);

};

class FSM
{
public:
	FSM();
	~FSM();

	virtual void Update(Agents agent);
	virtual void ChangeState(Agents agent, State newState);
	virtual void GetCurrentState();
	virtual void GetPreviousState();

	State currentState;
	State prevState;

};

class WanderState : State
{
	virtual void Update(Agents agent, FSM sm);

};

*/